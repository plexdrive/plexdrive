workspace:
  base: /go
  path: src/github.com/dweidenfeld/plexdrive

pipeline:
  test:
    image: golang
    commands:
      - go get
      - go test ./...

  build:
    image: golang
    commands:
      - go get
      - go build
      - ./go-build-all.sh
    when:
      branch: [ master, develop ]